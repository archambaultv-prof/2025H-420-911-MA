---
sidebar_position: 1
sidebar_label: Environnement et Python
description: Environnement et Python
---


# 🌳 Variable d'environnement

- Seul, à votre rythme

## 🎯 Objectif pédagogique

- Utiliser les variables d'environnement dans PowerShell et Python.

## 📜 Énoncé


### Étape 1

Écrivez un script Python qui enregistre les variables d'environnement
dans un fichier JSON. Vous pouvez utiliser le module `os` pour accéder aux
variables d'environnement.

### Étape 2

- Lancez le script Python que vous avez écrit à l'étape 1 dans PowerShell.
- Faites une copie du fichier JSON généré.
- Ajoutez une variable d'environnement dans PowerShell et relancez le script Python.
- Vérifiez que la variable d'environnement a bien été ajoutée dans le fichier JSON.

### Étape 3

Modifiez votre script pour qu'il lise la variable d'environnement `MYAPP_MODE`
qui sera soit `dev`, soit `prod`. En fonction de la valeur de cette variable,
le script doit afficher un message différent dans la console.

### Étape 4

Testez votre script en définissant la variable d'environnement `MYAPP_MODE` à
`dev` et en l'exécutant. Ensuite, changez la variable à `prod` et exécutez à
nouveau le script. Assurez-vous que le message affiché correspond à la valeur
de la variable.